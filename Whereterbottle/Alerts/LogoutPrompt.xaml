<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    x:Class="Whereterbottle.Alerts.LogoutPrompt"
    x:Name="LogoutPromptWindow">

    <Frame CornerRadius="10"
        OutlineColor="LightSkyBlue"
        HorizontalOptions="Center"
        VerticalOptions="Center"
        HasShadow="True"
        IsClippedToBounds="True"
        Padding="0">
        <StackLayout
        BackgroundColor="LightSkyBlue"
        VerticalOptions="Center"
        HorizontalOptions="Center"
        Padding="20, 20, 20, 20">
            <Label Text="Are you sure?" TextColor="White" HorizontalOptions="Center" />
            <StackLayout Orientation="Horizontal">
                <Button x:Name="btnOK" Clicked="btnOK_Clicked" BackgroundColor="DodgerBlue" Text="Yes" TextColor="White" />
                <Button x:Name="btnNo" Clicked="btnNo_Clicked" BackgroundColor="DodgerBlue" Text="No" TextColor="White" />
            </StackLayout>
        </StackLayout>
    </Frame>
</pages:PopupPage>