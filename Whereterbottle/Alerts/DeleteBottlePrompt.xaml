<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    x:Class="Whereterbottle.Alerts.DeleteBottlePrompt"
    x:Name="DeleteBottlePromptWindow">

    <Frame CornerRadius="10"
        OutlineColor="LightSkyBlue"
        HorizontalOptions="Center"
        VerticalOptions="Center"
        HasShadow="True"
        IsClippedToBounds="True"
        Padding="0">
        <StackLayout
        BackgroundColor="LightSkyBlue"
        VerticalOptions="Center"
        HorizontalOptions="Center"
        Padding="20, 20, 20, 20">
            <Label Text="Bottle and bottle data deleted." TextColor="White" HorizontalOptions="Center" />
            <StackLayout HorizontalOptions="Center">
                <Button x:Name="okayBtn" Clicked="okayBtn_Clicked" BackgroundColor="DodgerBlue" Text="Okay" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" />
            </StackLayout>
        </StackLayout>
    </Frame>
</pages:PopupPage>